let storedNums = [];
let storedOperators = [];
let num = "";

(숫자버튼 클릭 이벤트)
자릿수에 눌린 숫자 추가
num += 눌린숫자;

(연산자 클릭 이벤트)
연산자 나오기 전까지의 숫자를 배열에 저장하고 num초기화.
storedNum.push(num);
num = "";
연산자 배열에 저장.
storedOperators.push(연산자);

연산자 여러번 클릭시 기존 배열에서 pop()하고 새로운 연산자 push()
acculmulator 업데이트.


(=버튼 클릭 이벤트)
if (stringNum.length) {
 숫자 배열에 저장.
 stringNum 초기화.
} else {
storedOperators.pop();
위에처럼 처리하거나 숫자배열만큼만 결과 계산하는 방법도 존재.
}
저장된 배열 이용하여 결과 계산
결과 업데이트.





(clear 버튼 클릭 이벤트)
배열 초기화.
num 초기화.
display 빈화면으로 업데이트.

(delete 버튼 클릭 이벤트)
num에서 마지막 자리 숫자 제거.
display 업데이트.

